version: '3.8'

services:
  worldforge:
    build: .
    ports:
      - "8501:8501"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    restart: unless-stopped
    # Optional: persist generated worlds
    # volumes:
    #   - ./data:/app/data
